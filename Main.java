import controller.SlidePuzzleController;

public class Main {
    public static void main(String[] args) {
        SlidePuzzleController controller = new SlidePuzzleController();
        controller.start();
    }
}
